play.modules.enabled += com.frank.lagomtest.preferences.impl.PreferencesModule

lagom.persistence.ask-timeout = 10s

preferences.cassandra.keyspace = preferences

cassandra-journal.keyspace = ${preferences.cassandra.keyspace}
cassandra-snapshot-store.keyspace = ${preferences.cassandra.keyspace}
lagom.persistence.read-side.cassandra.keyspace = ${preferences.cassandra.keyspace}
